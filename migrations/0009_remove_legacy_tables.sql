-- Remove legacy tables from old tableaux system (no longer used)
-- These were replaced by the hotesse system (calendars + privatisations)

DROP TABLE IF EXISTS rows;
DROP TABLE IF EXISTS table_params;
DROP TABLE IF EXISTS tables;
